# Banana Check Flutter app

This mobile-only Flutter workspace is part of the Banana Check monorepo. Run
the setup and quality-gate commands from the repository root; see the root
`README.md` for prerequisites and exact commands.

The current application deliberately uses `MockInferenceService`. Camera
capture, local persistence, and real TensorFlow Lite inference remain future
feature work described in `PROJECT_PLAN.md`.
