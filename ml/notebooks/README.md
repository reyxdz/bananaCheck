# Experiment notebooks

Use this directory for exploratory model work. Clear cell outputs before
sharing a notebook, never embed raw dataset images, and move reusable
preprocessing, training, conversion, or evaluation logic into Python modules.
