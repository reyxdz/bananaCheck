# Flowchart artifacts

Store source diagrams and exported figures used by the paper in this directory.
Prefer editable source plus a reviewable export, use descriptive names, and do
not commit generated build caches.

The two foundational flows are documented as text diagrams in
`docs/ARCHITECTURE.md`. Add formal figures here only when the paper or project
documentation needs them.
